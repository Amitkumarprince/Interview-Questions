//cookies:- JavaScript cookies are small data stored on a user's device by a web browser.

// encodeURIComponent :- The encodeURIComponent() method encodes special characters including:
//  $ # @  & = + , / ? :

// decodeURIComponent :- The decodeURIComponent() function decodes a URI (Uniform Resource Identifier) component previously created by encodeURIComponent() or by a similar routine.

// localStorage :- localStorage in JavaScript allows web applications to store data locally
// within the user's browser – with no expiration date.
// The data is not deleted when the browser is closed, and is available when the browser is opened again.

// What is a key and a value?
// key() : A key, which is a constant that defines the data set (e.g., gender, color, price),
// value :- and a value, which is a variable that belongs to the set. (e.g., male/female, green, 100).

// setItem() : The setItem() method is used to / Adds key and value to localStorage.

// getItem() : The getItem() method is used to / Re'trieves/gets items from localStorage.
//                                               पुनः प्राप्त करना
//  removeItem() : The removeItem() method is used to / Removes an item from localStorage.

// clear() : The clear() method is used to / Clears all data from localStorage.


// sessionStorage: - sessionStorage is similar to localStorage; but data in sessionStorage is cleared when the page session ends

// 14.  What do you mean by Self Invoking Functions? (IIFE)
// Immediately Invoked Function Expression, is a JavaScript function that run immediately after it is defined.

// Destructuring :- Object Destructuring or destructuring assignment /
//  Destructuring is an approach by which the properties from an object or values from an array can be directly unpacked to different variables.
// Destructuring एक ऐसा दृष्टिकोण है जिसके द्वारा /किसी ऑब्जेक्ट के गुणों या किसी सरणी के मानों को सीधे अलग-अलग वेरिएबल्स में अनपैक किया जा सकता है।

// spread operator :- The spread operator in JavaScript is used to unpack elements from arrays or properties from objects.
//      जावास्क्रिप्ट में स्प्रेड ऑपरेटर का उपयोग सरणियों से तत्वों या वस्तुओं से गुणों को अनपैक करने के लिए किया जाता है।

//                           क्लोजर एक साथ बंडल किए गए फ़ंक्शन का संयोजन है / इसके आसपास की स्थिति के संदर्भ में
// closure :- closure is the combination of a function bundled together  with references to its surrounding state (the lexical environment).
