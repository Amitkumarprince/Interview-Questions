// html = HyperText Markup Language.
// Hypertext = "Hypertext" refers to links that connect web pages to one another, either within a single website or between websites.
// markup language = In HTML, markup refers to the tags used to define elements within a document.

// html :- html is the standard markup language for creating web pages. HTML describe the structure of a web pages.
// HTML consists of a series of elements. HTML elements tell the browser how to display the content.

// Q:-1 What is DOCTYPE?
// A: DOCTYPE is an instruction to the web browser about the version of HTML the page is written in.

// Q:-2 HTML 4 and HTML5 differences?
// A: HTML5 includes new elements (e.g., <article>, <section>), audio and video support, and improved form controls compared to HTML4.


// how many tag in html
// There are 142 HTML tags within the latest HTML version.

// Q:-3 div and span difference?
// A: <div> is a block-level element, while <span> is an inline element.

// html5 latest tag
// Some key new tags introduced in HTML5 include <article>, <aside>, <audio>, <canvas>, <details>, <embed>, <figure>, <footer>, <header>, <mark>, <meter>, <nav>, and <video>.

// form Elements
// The <form> element is a container for different types of input elements, such as: text fields, checkboxes, radio buttons, submit buttons, etc.

// Q:-4 Form elements tags in HTML5?
// A: New form elements include <datalist>, <output>, <progress>, <meter>, <keygen>, and <input type='date>

// Q:-5 Semantic tags in HTML5?
// A: Examples include <article>, <section>, <nav>, <header>, and <footer>

// semantic tag and due tag me diff
// seo tags

// Q:-6 meta tag in HTML5?
// A: Provides metadata about the HTML document, such as charset, viewport settings, and keywords

// Q:-7 title tag in HTML?
// A: Defines the title of the document, shown in the browser's title bar or tab.


// Q:-8 Head and body content?
// A: The <head> contains metadata, links to stylesheets, and scripts; the <body> contains the actual content of the
// webpage.


// Q:-9 Favicon?
// A: A small icon displayed in the browser tab, usually linked in the <head> with <link rel='icon'>.


// Q:-10 Structured HTML?
// A: Organized and well-nested HTML that improves readability and maintainability.

// head ke andar sequence


// Q:-11 alt attribute in image?
// A: Provides alternative text for an image if it cannot be displayed.

// Q:-12 h1 to h6 font sizes?
// A: Font sizes typically decrease from <h1> (largest) to <h6> (smallest), with default sizes being browser-dependent.
